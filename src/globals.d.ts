declare global {
  const __DEV__: boolean;
  // Add other global variables specific to React Native if needed
}

// Export {} to make this file a module (avoids isolatedModules error if empty)
export {};
